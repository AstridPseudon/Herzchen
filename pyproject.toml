[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "herzchen-contracts"
version = "0.1.0"
description = "Neutral FND-02 contracts for Herzchen composition"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
